apiVersion: v1
kind: Service
metadata:
  name: s3
  labels:
    app: s3
spec:
  type: ClusterIP
  selector:
    app: s3
  ports:
    - name: http
      port: 80
      targetPort: 9000
      protocol: TCP